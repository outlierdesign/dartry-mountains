// Font configuration for the Dartry Mountains website
// In production: use next/font/google with Playfair Display + Source Sans 3
// For build without Google Fonts access, we use CSS fallback stacks defined in globals.css

// These variables match the CSS custom properties in globals.css
export const playfairDisplay = {
  variable: "--font-display",
};

export const sourceSans = {
  variable: "--font-body",
};
